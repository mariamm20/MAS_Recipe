<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:tools="http://schemas.android.com/tools">

    <uses-permission android:name="android.permission.ACCESS_NETWORK_STATE" />
    <uses-permission android:name="android.permission.INTERNET" />

    <application
        android:allowBackup="true"
        android:dataExtractionRules="@xml/data_extraction_rules"
        android:fullBackupContent="@xml/backup_rules"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:roundIcon="@mipmap/ic_launcher_round"
        android:supportsRtl="true"
        android:theme="@style/Theme.MyApplication"
        tools:targetApi="31">
        <activity
            android:name=".Registration.EditProfileActivity"
            android:exported="false"
            android:theme="@style/no_status_bar"/>
        <activity
            android:name=".Home.SearchActivity"
            android:exported="false"
            android:theme="@style/no_status_bar" />
        <activity
            android:name=".RecipeDetails.RecipeDetailsActivity"
            android:exported="false"
            android:theme="@style/no_status_bar" />
        <activity
            android:name=".Home.HomeActivity"
            android:exported="false"
            android:theme="@style/no_status_bar" />
        <activity
            android:name=".Registration.ForgetPasswordActivity"
            android:exported="false"
            android:theme="@style/no_status_bar" />
        <activity
            android:name=".Registration.SignUpActivity"
            android:exported="false"
            android:theme="@style/no_status_bar" />
        <activity
            android:name=".Registration.LoginActivity"
            android:exported="false"
            android:theme="@style/no_status_bar" />
        <activity
            android:name=".WelcomeScreens.OnboardingActivity"
            android:exported="false"
            android:theme="@style/no_status_bar" />
        <activity
            android:name=".WelcomeScreens.SplashScreenActivity"
            android:exported="true"
            android:theme="@style/no_status_bar">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />
                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>
    </application>

</manifest>